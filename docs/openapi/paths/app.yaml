"/":
  get:
    tags:
      - 应用
    summary: 基础探活接口
    description: 返回 `AppService.getHello()` 输出的纯文本内容。
    operationId: getHello
    security: []
    responses:
      '200':
        description: 纯文本问候语
        content:
          text/plain:
            schema:
              type: string
              example: Hello World!
